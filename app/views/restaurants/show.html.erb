<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant) %>, <%= restaurant.address %>, <%= restaurant.phone %>, <%= restaurant.description %>, <%= restaurant.category_id %></p>
<% end %>
